{
  "name": "obsidian-mindmap-jsmind",
  "version": "0.1.0",
  "description": "Obsidian plugin to preview and edit markdown as a jsMind mindmap",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild main.ts --bundle --external:obsidian --format=cjs --platform=browser --outfile=main.js --sourcemap",
    "build": "esbuild main.ts --bundle --external:obsidian --format=cjs --platform=browser --outfile=main.js --sourcemap"
  },
  "keywords": [
    "obsidian-plugin",
    "mindmap",
    "jsmind"
  ],
  "author": "OpenMind",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "obsidian": "^1.5.0"
  }
}


